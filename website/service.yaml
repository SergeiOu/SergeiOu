apiVersion: v1
kind: Service
metadata:
  name: django-service
spec:
  type: NodePort                   # exposes your app externally via Minikube
  selector:
    app: django-app
  ports:
    - protocol: TCP
      port: 80                     # public port
      targetPort: 8000             # container’s port (from above)
